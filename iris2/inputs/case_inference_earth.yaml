case:
  name: case_inference_earth

job:
  type: inference
  accelerators:
    count: 8
    type: gpu
  hardware:
    min_memory_gb: 24

objectives:
  - minimize_latency

constraints:
  latency_sensitivity: strict
  energy_preference: balanced
  reliability: high

placement:
  preference: earth

network:
  preference: packet

runtime_policy:
  conservative: true
  allow_hybrid: false
