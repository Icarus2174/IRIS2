case:
  name: case_archive_orbit

job:
  type: archive
  accelerators:
    count: 0
    type: none
  hardware:
    min_memory_gb: 8

objectives:
  - minimize_energy
  - prefer_space_compute

constraints:
  latency_sensitivity: none
  energy_preference: lowest_energy
  reliability: medium

placement:
  preference: space

network:
  preference: packet

runtime_policy:
  conservative: true
  allow_hybrid: true
